services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: canigo
      POSTGRES_USER: canigo
      POSTGRES_PASSWORD: canigo
    ports:
      - "5432:5432"
    volumes:
      - canigo_pgdata:/var/lib/postgresql/data

volumes:
  canigo_pgdata: